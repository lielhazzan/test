// פונקציה שמופעלת בלחיצה על הכפתור ב-index.html
function showAlert() {
    console.log("Button clicked!"); // זה מופיע בתוך ה-Console של הדפדפן (F12)
    alert("Connection Secure: Dashboard is fully interactive and users are saved!");
}